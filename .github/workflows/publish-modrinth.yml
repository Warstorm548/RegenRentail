name: Publish to Modrinth

on:
  push:
    branches: [ "main" ]
    paths-ignore:
      - '**.md'
      - 'docs/**'
      - '.gitignore'
      - 'LICENSE'
  pull_request:
    branches: [ "main" ]
    types: [ closed ]
    paths-ignore:
      - '**.md'
      - 'docs/**'
      - '.gitignore'
      - 'LICENSE'

jobs:
  publish:
    runs-on: ubuntu-latest
    # Run on push OR on merged PR (not just closed)
    if: github.event_name == 'push' || (github.event_name == 'pull_request' && github.event.pull_request.merged == true)

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up JDK 21
      uses: actions/setup-java@v4
      with:
        java-version: '21'
        distribution: 'temurin'
        cache: 'gradle'

    - name: Setup Gradle 9.2.0
      uses: gradle/actions/setup-gradle@v3
      with:
        gradle-version: '9.2.0'

    - name: Grant execute permission for gradlew
      run: chmod +x gradlew

    - name: Build with Gradle
      run: ./gradlew clean build

    - name: Extract version from build.gradle.kts
      id: version
      run: |
        VERSION=$(grep -oP 'version\s*=\s*"\K[^"]+' build.gradle.kts)
        echo "version=$VERSION" >> $GITHUB_OUTPUT
        echo "Extracted version: $VERSION"

    - name: Publish to Modrinth
      uses: Kir-Antipov/mc-publish@v3.3
      with:
        modrinth-id: iNHRLu42
        modrinth-token: ${{ secrets.MODRINTHAPI }}

        files: build/libs/RegionRental-*.jar

        name: RegionRental v${{ steps.version.outputs.version }}
        version: ${{ steps.version.outputs.version }}
        version-type: alpha

        loaders: |
          paper
          spigot
          bukkit
          purpur

        game-versions: |
          1.21
          1.21.1
          1.21.3

        changelog-file: CHANGELOG.md
